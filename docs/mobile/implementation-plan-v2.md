# 移动端架构重构实施计划 v2

## 0. 执行规则

### 0.1 状态

- `[ ]` 未开始。
- `[~]` 进行中。
- `[x]` 已完成且有证据。
- `[!]` 阻塞。
- `[-]` 取消并有 ADR。

### 0.2 每项关闭证据

每个任务必须记录：

```text
任务编号：
状态：
修改文件：
测试命令：
测试结果：
产物路径：
剩余风险：
关联 ADR：
```

### 0.3 Git 规则

- 每一阶段单独分支或独立逻辑提交。
- 不把删除本地 AI、Provider 重构和移动 UI 混进一个不可审查提交。
- 未经用户明确要求不得提交或推送。
- 提交前检查暂存 diff、测试证据和秘密扫描。

## G0 文档与基线冻结

### G0-01 确认工作树

- [ ] 运行 `git status --short --branch`。
- [ ] 记录当前分支、远程和未跟踪文件。
- [ ] 不覆盖 `.superpowers/brainstorm` 和历史文档。
- [ ] 确认实施是在目标仓库而不是旧 worktree。

**验收**：工作树来源明确，非本任务改动有清单。

### G0-02 建立基线证据

- [ ] 运行版本同步检查。
- [ ] 运行后端核心测试。
- [ ] 运行前端 build。
- [ ] 记录既有失败和环境问题。
- [ ] 保存当前路由、Provider、本地 AI 引用扫描结果。

**停止条件**：无法区分既有失败与新增失败。

### G0-03 冻结决策

- [ ] 阅读 `docs/mobile` 六份文档。
- [ ] 确认旧移动方案已被 v2 取代。
- [ ] 确认 Termux 只是远程 CLI 承载。
- [ ] 确认本地 AI 与 Ollama 都要删除。
- [ ] 确认所有其他功能入口保留。

**验收**：实现者可以用一句话复述目标架构，且不包含端内 AgentCore。

## G1 本地 AI 与 Ollama 退役

### G1-01 先写退役扫描测试

- [ ] 新建 `tests/test_local_ai_retirement.py`。
- [ ] 定义生产目录扫描范围。
- [ ] 定义历史文档和迁移测试 allowlist。
- [ ] 让测试先因现有引用失败。

**验收**：测试能同时捕获 Ollama、本地部署页面、本地 Embedding 和模型资产。

### G1-02 设计配置迁移

- [ ] 列出所有 Ollama 与 local deploy 配置键。
- [ ] 定义单 Provider、多 Provider、无 Provider 三种迁移结果。
- [ ] 写配置备份和回滚测试。
- [ ] 写占位凭证删除测试。
- [ ] 写启动恢复不再注册 Ollama 的测试。

**验收**：旧用户配置升级后不会指向不存在的 Provider。

### G1-03 实现向量迁移工具

- [ ] 读取向量维度和表清单。
- [ ] 验证远程 Embedding 配置。
- [ ] 备份数据库。
- [ ] 在临时库批量重编码。
- [ ] 实现断点和限流退避。
- [ ] 校验行数、向量和抽样召回。
- [ ] 原子替换并支持恢复。

**验收**：512→目标维度迁移成功；中途失败恢复原库。

### G1-04 删除前端本地部署

- [ ] 删除路由。
- [ ] 删除侧栏入口。
- [ ] 删除页面。
- [ ] 删除中英文文案。
- [ ] 删除移动能力表中的相关候选项。
- [ ] 验证深链访问返回明确 404 或迁移提示。

**验收**：桌面和移动导航均不存在本地部署入口。

### G1-05 删除后端本地部署

- [ ] 删除 router 导入和注册。
- [ ] 删除 local deploy API。
- [ ] 删除 local deploy config schema。
- [ ] 删除本地引擎启动、停止和设备探测。
- [ ] 删除日志过滤路径。

**验收**：`/api/v1/local-deploy/*` 不再存在，服务正常启动。

### G1-06 删除本地 Embedding

- [ ] 删除 LocalEmbedProvider。
- [ ] 删除 VectorStore 本地分支。
- [ ] 删除模型资产。
- [ ] 删除 ONNX/tokenizers 专属依赖。
- [ ] 删除发布物模型检查。
- [ ] 调整 bootstrap 为 remote/disabled。
- [ ] 删除旧重建脚本或替换为远程迁移脚本。

**验收**：安装包、Docker 镜像和源码不包含 ONNX 模型与 runtime。

### G1-07 删除 Ollama

- [ ] 删除 metadata。
- [ ] 删除 Setup 字段和测试。
- [ ] 删除 server 自动注册和恢复。
- [ ] 删除 ModelRouter 特判。
- [ ] 删除免费成本与发现特判。
- [ ] 删除 `.env.example` 变量。
- [ ] 更新当前文档。

**验收**：生产扫描零命中；旧配置迁移测试通过。

### G1-08 回归

- [ ] 启动无远程 Embedding 的降级模式。
- [ ] 启动有远程 Embedding 的完整模式。
- [ ] 验证文本记忆仍可用。
- [ ] 验证语义检索状态可见。
- [ ] 验证其他 Provider 不受影响。

**门禁**：G1 全部通过才能开始 Provider 大重构。

## G2 自定义服务商重构

### G2-01 错误协议

- [ ] 定义稳定错误码、stage 和 retryable。
- [ ] 统一 FastAPI 错误外形。
- [ ] 修改前端 API 读取顶层 `message`。
- [ ] 保持旧客户端可读 `detail` 的兼容期。

**验收**：认证、超时、DNS、TLS、限流、模型不存在和 SSRF 均有稳定结果。

### G2-02 URL 规范化

- [ ] 明确 base URL 是 API 根还是站点根。
- [ ] 处理末尾斜杠和已有 `/v1`。
- [ ] 拒绝 userinfo、非法端口和危险编码。
- [ ] 支持自定义路径前缀。
- [ ] 保证 discover 与 chat 使用同一规范化结果。

**验收**：根域、`/v1`、路径前缀、IPv6 和非法输入表驱动测试通过。

### G2-03 安全出站客户端

- [ ] 将 DNS 校验和真实连接合并。
- [ ] 使用 pinned IP，同时保持原 Host/SNI。
- [ ] 禁止跨主机重定向。
- [ ] 收紧私网和宿主别名豁免。
- [ ] 对端口和协议建立 allowlist。
- [ ] 统一用于发现、探针和聊天。

**验收**：DNS rebinding 真实连接测试通过。

### G2-04 Provider 应用服务

- [ ] 抽离 repository、credential store 和 runtime registry。
- [ ] 实现创建事务。
- [ ] 实现更新事务。
- [ ] 实现删除事务。
- [ ] 故障注入每个阶段。
- [ ] 验证无孤儿配置、密钥和客户端。

**验收**：所有故障点回到操作前状态。

### G2-05 引用完整性

- [ ] 收集 RouteRegistry 引用。
- [ ] 收集聊天模型引用。
- [ ] 收集全部 Agent 配置引用。
- [ ] 删除前返回引用列表。
- [ ] 禁用时立即停止新调用。

**验收**：被引用 Provider 不可被静默删除或假禁用。

### G2-06 模型发现

- [ ] 按 Provider 分片缓存。
- [ ] 区分成功和失败 TTL。
- [ ] 返回每个 Provider 状态。
- [ ] 支持手动模型。
- [ ] 分类展示非聊天模型。
- [ ] 标记能力证据来源。

**验收**：一个 Provider 故障不影响其他 Provider，且错误不再表现为空模型列表。

### G2-07 连接测试

- [ ] 分开验证 DNS、TLS、认证、模型发现和聊天 probe。
- [ ] 临时客户端显式关闭。
- [ ] 增加分阶段超时。
- [ ] 过滤上游敏感正文。
- [ ] 连续执行 100 次资源不增长。

**验收**：UI 能定位失败阶段，服务端无连接泄漏。

### G2-08 Provider 前端

- [ ] 拆分 Provider 列表、编辑器、诊断和模型选择器。
- [ ] API Key 只替换不回填。
- [ ] 展示规范化 URL。
- [ ] 支持先测试后保存。
- [ ] 支持手动模型 ID。
- [ ] 显示引用冲突和迁移操作。
- [ ] 提供移动全屏编辑体验。

**验收**：360dp 下可以完成创建、测试、发现、保存、编辑、禁用和删除。

### G2-09 Provider CI

- [ ] 新增 CRUD 集成测试。
- [ ] 新增事务故障注入。
- [ ] 新增安全出站测试。
- [ ] 新增发现与错误映射测试。
- [ ] 加入 strict CI。

**门禁**：关键 Provider 测试不得 continue-on-error。

## G3 共享导航与移动壳

### G3-01 导航事实源

- [ ] 新建 `capabilities.ts`。
- [ ] 录入全部现有生产路由。
- [ ] 为每项定义分组、排序、图标和移动位置。
- [ ] SideBar 改为读取能力表。
- [ ] 路由与能力表建立一致性测试。

**验收**：新增或删除路由时一致性测试会失败。

### G3-02 响应式壳

- [ ] 抽离 DesktopShell。
- [ ] 实现 MobileAppShell。
- [ ] AppLayout 只负责壳选择和共享背景。
- [ ] 宽度变化不销毁业务 Store。
- [ ] 横竖屏切换不丢失页面状态。

**验收**：断点切换不重复初始化 Chat、WS 或终端。

### G3-03 底部五导航

- [ ] 聊天、Agent、记忆、工具、设置。
- [ ] 使用极光玻璃 SVG。
- [ ] 48dp 热区和安全区。
- [ ] 选中、按下、禁用和焦点状态。
- [ ] 屏幕阅读器标签。

**验收**：单手可达、无误触、状态不只靠颜色。

### G3-04 全部能力抽屉

- [ ] 左上角入口。
- [ ] 四类能力分组。
- [ ] 支持搜索。
- [ ] 当前路由高亮。
- [ ] 跳转后自动关闭。
- [ ] 所有现有页面均可达。

**验收**：路由覆盖测试为 100%。

### G3-05 快捷状态

- [ ] 当前 Agent。
- [ ] WS 连接状态。
- [ ] 当前 Provider/模型。
- [ ] 通知和快捷设置。
- [ ] 离线、降级和模型未配置状态。

**验收**：状态来源真实，不用静态假数据。

## G4 页面、终端和壁纸

### G4-01 逐页移动适配

对 `change-spec-v2.md` 的页面表逐项执行：

- [ ] 消除固定宽度和不可见横向内容。
- [ ] 将复杂编辑器改为全屏或 Sheet。
- [ ] 表格提供卡片或可控横向滚动。
- [ ] 操作按钮进入触控可达位置。
- [ ] 错误、空状态和加载状态完整。
- [ ] 保留桌面布局质量。

**验收**：每个路由在四个移动视口和桌面视口截图通过。

### G4-02 终端移动 Sheet

- [ ] 保留右下角 FAB。
- [ ] 移动端改底部 Sheet。
- [ ] 处理键盘、旋转和 safe area。
- [ ] 保留多会话。
- [ ] 关闭确认和状态同步。
- [ ] 服务端进程树回收测试。

**验收**：终端会话不因 UI 重建重复或泄漏。

### G4-03 壁纸数据扩展

- [ ] 增加焦点、遮罩和动效字段。
- [ ] 兼容旧配置。
- [ ] API 验证范围。
- [ ] 上传后焦点编辑。
- [ ] 默认壁纸迁移。

**验收**：旧用户壁纸不丢失，新字段可回滚。

### G4-04 壁纸渲染

- [ ] 背景不随键盘缩放。
- [ ] 局部磨砂。
- [ ] 亮暗对比度适配。
- [ ] 性能三级降级。
- [ ] 减少动画偏好。
- [ ] 低内存时资源释放。

**验收**：关键文字可读，交互流畅，无明显背景跳动。

## G5 Android 系统壳

### G5-01 工程初始化

- [ ] 创建 Kotlin DSL 工程。
- [ ] 建立四个模块。
- [ ] 配置 debug/staging/release。
- [ ] 固定 JDK、AGP 和依赖目录。
- [ ] 加入 lint、test、assemble CI。

**验收**：空壳三变体可构建。

### G5-02 Web 资源集成

- [ ] 前端 build 产物复制到 Android assets。
- [ ] WebViewAssetLoader 提供受信任 origin。
- [ ] 构建时校验资源哈希。
- [ ] 禁止运行时下载替换主 UI。
- [ ] 错误页可显示版本和诊断 ID。

**验收**：离线可打开 UI 壳，在线后连接远端服务。

### G5-03 认证和 Bridge

- [ ] Token 进入 Keystore 保护存储。
- [ ] Web 页面只获得短期会话句柄。
- [ ] Bridge 来源校验。
- [ ] 文件、分享、终端接口参数校验。
- [ ] 日志脱敏。

**验收**：Web Storage、URL、日志和崩溃报告不含明文 token。

### G5-04 系统能力

- [ ] 文件选择和上传。
- [ ] 分享。
- [ ] 深链。
- [ ] 返回手势。
- [ ] 网络状态。
- [ ] 通知权限和通知跳转。
- [ ] 前后台连接策略。

**验收**：系统行为符合 Android 生命周期，不使用伪保活。

### G5-05 Termux/终端预研门禁

- [ ] 冻结具体组件和来源。
- [ ] 生成 BOM、SBOM、NOTICE。
- [ ] 法务确认许可证义务。
- [ ] 商店政策预审。
- [ ] 实现 RuntimeSupervisor 状态机 PoC。
- [ ] 测试 ABI、包体、安装空间。
- [ ] 测试单实例和进程树回收。

**停止条件**：任一发布义务不清晰，正式包禁用该模块。

### G5-06 远程 CLI 自动启动

- [ ] CLI 禁止自动拉起本地 Web/Agent 服务。
- [ ] CLI 使用远端 HTTPS/WSS 地址。
- [ ] 实例 nonce 和协议版本握手。
- [ ] 用户可停止和查看状态。
- [ ] 崩溃退避，禁止无限重启。
- [ ] 不持有 Provider Key。

**验收**：进程重建、多窗口和快速切换均保持单实例。

## G6 集成、发布和回滚

### G6-01 全量测试

- [ ] 后端全量 pytest。
- [ ] Ruff 和版本同步。
- [ ] 前端 typecheck、单测、build、E2E。
- [ ] Android lint、test、assemble、仪器测试。
- [ ] 敏感信息扫描。
- [ ] 本地 AI/Ollama 防复活扫描。

### G6-02 性能

- [ ] 冷启动。
- [ ] Chat 首屏。
- [ ] 长会话滚动。
- [ ] 图谱和仪表盘降级。
- [ ] 壁纸和粒子性能。
- [ ] 终端持续输出。
- [ ] 内存压力恢复。

### G6-03 兼容

- [ ] 最低 Android 版本。
- [ ] 当前 Android 稳定版本。
- [ ] 刘海、打孔、手势导航。
- [ ] 深色/浅色和字体缩放。
- [ ] 弱网、断网、切网。
- [ ] 服务端旧版本和能力不匹配。

### G6-04 发布物

- [ ] 签名 AAB/APK。
- [ ] 前端资源哈希。
- [ ] SBOM 和许可证归档。
- [ ] 混淆映射与 native symbols。
- [ ] 版本兼容矩阵。
- [ ] 回滚包和回滚说明。

### G6-05 最终验收

- [ ] `acceptance-matrix-v2.md` 全部 P0/P1 通过。
- [ ] 例外项有负责人、原因和到期日。
- [ ] 文档与代码一致。
- [ ] 新上下文按 handoff runbook 可独立复现验证。

**完成条件**：所有门禁通过后才允许标记发布候选。

