# ── Web 服务 ──
fastapi>=0.115.0
starlette>=0.40.0
uvicorn[standard]>=0.32.0
websockets>=13.0
python-multipart>=0.0.12
sse-starlette>=2.1.0
structlog>=24.0.0

# ── HTTP 客户端 ──
httpx[http2]>=0.27.0
httpcore>=1.0.9,<1.1
requests>=2.31.0

# ── 图像处理 ──
Pillow>=10.4.0
qrcode>=7.4.0

# ── 数值计算 ──
numpy>=1.26.0,<3

# ── CLI 美化 ──
rich>=13.9.0

# ── CLI 交互补全（/ 弹出下拉 + 多步菜单选择）──
prompt-toolkit>=3.0.0

# ── SILK 音频编码 ──
pilk>=0.2.4

# ── 系统监控 ──
psutil>=6.0.0
# ── Linux 发行版识别（prompt_builder/web 系统信息，缺失时降级 platform.platform）──
distro>=1.9.0

# ── 配置热重载: watchdog 监听 config/ 文件变更，缺失时降级为无热重载 ──
watchdog>=4.0.0

# ── 可观测性: Prometheus 指标导出 (/metrics 端点) ──
prometheus-client>=0.20.0

# ── 核心 ──
python-dotenv>=1.2.2
loguru>=0.7.3
openai>=2.41.0
aiosqlite>=0.20.0
pydantic>=2.13.4
jieba>=0.42.1
rapidfuzz>=3.0.0
pyyaml>=6.0
cryptography>=43.0.0
certifi>=2024.7.0

# ── 向量检索: sqlite-vec 提供子chunk向量召回，缺失时降级为纯FTS ──
sqlite-vec>=0.1.0

# ── QQ Bot ──
qq-botpy>=1.2.1

# ── 搜索（可选，需 TAVILY_API_KEY）──
# tavily-python>=0.5.0
# primp>=0.7.0

# ── HTML 解析: 搜索工具（知乎/网页提取）依赖 lxml，无 try-except 保护 ──
lxml>=5.0.0

# ── 文档处理 ──
pdfplumber>=0.11.9
python-docx>=1.2.0
python-pptx>=1.0.2
openpyxl>=3.1.5
html2text>=2025.4.15

# ── 桌面模式（Windows pywebview 原生窗口）──
pywebview>=5.0

# ── 可选: Streamlit Web UI (旧版) ──
streamlit>=1.28.0

# ── v0.6.0 认知架构 ──
networkx>=3.2

# ── JSON5 配置文件解析 ──
json5>=0.9.0
